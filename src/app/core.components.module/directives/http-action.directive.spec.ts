import { HttpActionDirective } from './http-action.directive';

describe('HttpActionDirective', () => {
  it('should create an instance', () => {
    const directive = new HttpActionDirective();
    expect(directive).toBeTruthy();
  });
});
